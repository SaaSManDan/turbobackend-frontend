Notes:
- Add env keys
- Add project name to logsnag error logger
- Ensure user database table is created in local pg db
- Update name field in package.json
- Remove .git and re-initiate using the following steps
    - rm -rf .git
    - git init
- Ensure tailwind is working properly